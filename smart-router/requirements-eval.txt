# Local eval scripts, dashboard, and optional ML tooling — not installed in Docker.
-r requirements.txt

# Dashboard
streamlit==1.39.0
pandas==2.2.3
plotly==5.24.1

# Eval / dataset build scripts (build_eval_dataset.py, etc.)
datasets==2.21.0
huggingface_hub==0.24.0

# Only if you run plotting or sklearn-based analysis locally
scikit-learn==1.5.2
matplotlib==3.9.2
seaborn==0.13.2
